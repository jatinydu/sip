export enum UserTypes {
  BUSINESS = "business",
  CUSTOMER = "customer",
  BRANCH_OWNER = "branch_owner",
}

export enum OtpPurpose {
    SIGNUP = "signup",
    FORGET_PASSWORD = "forget_password",
}